

{
  "name": "project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "http-server ./static -p 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "http-server": "^0.12.3"
  }
}